name: preprocess_land
expts:
  - xqhuc
  - xqhud
paths:
  raw_root: ${SCRIPTS_ROOT}/
  cache_root: ${SCRIPTS_ROOT}/cache
  output_root: ${SCRIPTS_ROOT}/outputs
preprocess:
  regions: [global]
  n_years: null
variables:
  soilResp: "S resp"
  soilCarbon: "S carb"
  VegCarb: "V carb"
  GPP: "GPP"
  NPP: "NPP"
  # fgco2: "Ocean flux"
  # tas: "temp"
  # pr: "precip"
